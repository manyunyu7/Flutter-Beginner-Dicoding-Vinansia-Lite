import 'package:flutter/material.dart';

class SnipItemWidget extends StatelessWidget {
  const SnipItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
